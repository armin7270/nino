# ==============================================================================
# Nixpacks fallback build definition.
# ==============================================================================
[phases.setup]
nixPkgs = ["nodejs_22", "curl", "cacert"]

[phases.install]
cmds = ["npm install --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/server.cjs"

[variables]
NODE_ENV = "production"
ANYTLS_GATEWAY_PORT = "8443"
DATA_DIR = "/app/data"
ANYTLS_AUTO_DOWNLOAD = "true"